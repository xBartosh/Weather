spring:
  datasource:
    url: jdbc:mysql://localhost:3306/weather?useSSL=false&serverTimezone=Europe/Warsaw
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: admin
  jpa:
    show-sql: true
  sql:
    init:
      mode: always
  sendgrid:
    api-key: d50267040db8973bc7076575040989ea
